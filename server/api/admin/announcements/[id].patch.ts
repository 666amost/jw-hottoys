import { z } from "zod";
const schema = z.object({ label: z.string().trim().min(1).max(40), message: z.string().trim().min(2).max(180), href: z.string().max(300).nullable().optional(), active: z.boolean(), sortOrder: z.number().int(), startsAt: z.string().nullable().optional(), endsAt: z.string().nullable().optional() });
export default defineEventHandler(async (event) => { assertSafeMutation(event); await requireAdmin(event); const parsed = schema.safeParse(await readLimitedBody(event)); if (!parsed.success) apiError(422,"VALIDATION_ERROR","Announcement tidak valid."); await bindings(event).DB.prepare("UPDATE site_announcements SET label=?,message=?,href=?,active=?,sort_order=?,starts_at=?,ends_at=?,updated_at=datetime('now') WHERE id=?").bind(parsed.data.label,parsed.data.message,parsed.data.href??null,parsed.data.active?1:0,parsed.data.sortOrder,parsed.data.startsAt??null,parsed.data.endsAt??null,getRouterParam(event,"id")).run(); return { updated:true }; });
